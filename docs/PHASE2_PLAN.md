# 📋 Phase 2: 고객 서비스 개발 계획

Joy-Flower 프로젝트의 고객용 주문 시스템 구현 계획서입니다.

## 🎯 목표

실제로 꽃을 주문하고 결제할 수 있는 완전한 고객 서비스를 구현합니다.

---

## 📊 전체 구조

```
고객 주문 플로우:
홈페이지 → 상품 선택 → 장바구니 → 체크아웃(주문 정보 입력) → Stripe 결제 → 주문 완료

페이지 구조:
├─ / (홈) ✅ 완료
├─ /products/[id] (상품 상세) 🔜
├─ /cart (장바구니) 🔜
├─ /checkout (체크아웃) 🔜
├─ /order/success?session_id=xxx (주문 완료) 🔜
└─ /order/[orderNumber] (주문 조회) 🔜

API 구조:
├─ POST /api/checkout (Stripe Checkout Session 생성) 🔜
├─ POST /api/webhooks/stripe (결제 완료 웹훅) 🔜
└─ GET /api/orders/[orderNumber] (주문 조회) 🔜
```

---

## 📝 단계별 구현 계획

### Step 1: 장바구니 페이지 (`/cart`)

**기능:**
- 장바구니에 담긴 상품 목록 표시
- 상품별 수량 조절 (+/- 버튼)
- 개별 상품 삭제
- 전체 금액 계산 및 표시
- "Checkout" 버튼 → `/checkout`로 이동

**UI 컴포넌트:**
- 상품 목록 테이블 또는 카드
- 수량 조절 버튼
- 삭제 버튼
- 가격 요약 (Subtotal, Total)
- 체크아웃 버튼

**사용 기술:**
- Zustand (`useCart` 훅) - 이미 구현됨
- shadcn/ui Table 또는 Card
- Next.js Link

**예상 시간:** 30-40분

---

### Step 2: 상품 상세 페이지 (`/products/[id]`)

**기능:**
- URL 파라미터로 상품 ID 받기
- Supabase에서 해당 상품 정보 조회
- 상품 이미지, 이름, 설명, 가격, 재고 표시
- 수량 선택 (1-10개)
- "Add to Cart" 버튼
- "Back to Home" 버튼

**UI 컴포넌트:**
- 큰 상품 이미지 (placeholder)
- 상품 정보 섹션
- 수량 선택 드롭다운 또는 인풋
- 액션 버튼들

**데이터 흐름:**
```typescript
// app/products/[id]/page.tsx
1. URL에서 id 추출
2. Supabase에서 해당 상품 조회
3. 404 처리 (상품 없을 때)
4. 상품 정보 표시
```

**예상 시간:** 30-40분

---

### Step 3: 체크아웃 페이지 (`/checkout`)

**기능:**
- 장바구니가 비어있으면 → `/cart`로 리다이렉트
- 주문 정보 입력 폼:
  - 주문자 정보 (이름, 전화번호, 이메일)
  - 받는 사람 정보 (이름, 전화번호, 주소)
  - 배송 방법 (Pickup / Delivery)
  - 배송 날짜/시간 선택
  - 메시지 카드 내용 (선택사항)
- 주문 요약 (상품 목록, 총 금액)
- "Proceed to Payment" 버튼 → Stripe Checkout으로 이동

**UI 컴포넌트:**
- React Hook Form + Zod 검증
- shadcn/ui Form 컴포넌트
- Input, Textarea, Select, Radio
- 주문 요약 사이드바

**폼 검증:**
```typescript
- 이름: 최소 2자
- 전화번호: 10자리 이상
- 이메일: 유효한 이메일 형식
- 주소: 최소 10자
- 배송 날짜: 오늘 이후
```

**데이터 흐름:**
```typescript
1. 폼 입력
2. 검증 통과
3. POST /api/checkout 호출
4. Stripe Checkout Session ID 받기
5. Stripe Checkout 페이지로 리다이렉트
```

**예상 시간:** 60-90분

---

### Step 4: Stripe 결제 API (`/api/checkout`)

**기능:**
- 장바구니 데이터 + 주문 정보 받기
- Supabase에 주문 생성 (status: 'pending')
- Stripe Checkout Session 생성
- Session ID 반환

**API 엔드포인트:**
```typescript
POST /api/checkout

Request Body:
{
  orderData: {
    buyerName: string
    buyerPhone: string
    buyerEmail: string
    recipientName: string
    recipientPhone: string
    recipientAddress: string
    deliveryType: 'pickup' | 'delivery'
    deliveryDate?: string
    deliveryTime?: string
    messageCard?: string
  },
  items: CartItem[]
}

Response:
{
  sessionId: string  // Stripe Checkout Session ID
  orderId: string    // Supabase order ID
}
```

**구현 단계:**
1. 주문 번호 생성 (ORD-YYYYMMDD-XXXX)
2. Supabase에 주문 저장
3. Supabase에 주문 항목(order_items) 저장
4. Stripe Checkout Session 생성:
   ```typescript
   - line_items: 장바구니 상품들
   - mode: 'payment'
   - success_url: /order/success?session_id={CHECKOUT_SESSION_ID}
   - cancel_url: /checkout
   - metadata: { orderId, orderNumber }
   ```
5. Session ID 반환

**에러 처리:**
- 장바구니 비어있음
- 재고 부족
- Supabase 오류
- Stripe API 오류

**예상 시간:** 60-90분

---

### Step 5: Stripe 웹훅 API (`/api/webhooks/stripe`)

**기능:**
- Stripe에서 결제 완료 이벤트 수신
- 주문 상태 업데이트 (pending → confirmed)
- 결제 정보 저장

**API 엔드포인트:**
```typescript
POST /api/webhooks/stripe

처리 이벤트:
- checkout.session.completed
- payment_intent.succeeded

작업:
1. Stripe 서명 검증
2. 이벤트 타입 확인
3. metadata에서 orderId 추출
4. Supabase에서 주문 업데이트:
   - status: 'confirmed'
   - payment_status: 'paid'
   - stripe_payment_id: xxx
   - stripe_session_id: xxx
```

**보안:**
- Stripe 웹훅 서명 검증 필수
- 무단 요청 차단

**예상 시간:** 30-45분

---

### Step 6: 주문 완료 페이지 (`/order/success`)

**기능:**
- URL에서 `session_id` 파라미터 받기
- Stripe에서 세션 정보 조회
- metadata에서 주문 번호 추출
- 주문 완료 메시지 표시
- 주문 번호 표시
- "View Order" 버튼 → `/order/[orderNumber]`

**UI:**
- 성공 아이콘 (체크마크)
- "Thank you for your order!" 메시지
- 주문 번호 (강조)
- 안내 메시지
- 액션 버튼들

**예상 시간:** 20-30분

---

### Step 7: 주문 조회 페이지 (`/order/[orderNumber]`)

**기능:**
- URL에서 주문 번호 받기
- Supabase에서 주문 정보 + 주문 항목 조회
- 주문 상세 정보 표시:
  - 주문 번호
  - 주문 일시
  - 주문 상태
  - 상품 목록
  - 배송 정보
  - 총 금액
  - 결제 정보

**UI 컴포넌트:**
- 주문 헤더 (번호, 날짜, 상태)
- 상품 목록 테이블
- 배송 정보 카드
- 결제 정보 카드
- 상태 배지 (pending, confirmed, etc.)

**추가 API:**
```typescript
GET /api/orders/[orderNumber]

Response:
{
  order: Order
  items: OrderItem[]
}
```

**예상 시간:** 40-50분

---

## 🗂️ 파일 구조

```
app/
├── (customer)/              # 고객용 라우트 그룹
│   ├── page.tsx            # 홈 (이미 완료)
│   ├── products/
│   │   └── [id]/
│   │       └── page.tsx    # 상품 상세
│   ├── cart/
│   │   └── page.tsx        # 장바구니
│   ├── checkout/
│   │   └── page.tsx        # 체크아웃
│   └── order/
│       ├── success/
│       │   └── page.tsx    # 주문 완료
│       └── [orderNumber]/
│           └── page.tsx    # 주문 조회
│
└── api/
    ├── checkout/
    │   └── route.ts        # POST - Stripe Checkout 생성
    ├── webhooks/
    │   └── stripe/
    │       └── route.ts    # POST - Stripe 웹훅
    └── orders/
        └── [orderNumber]/
            └── route.ts    # GET - 주문 조회

components/
└── customer/               # 고객용 컴포넌트
    ├── CartItem.tsx       # 장바구니 항목
    ├── CartSummary.tsx    # 장바구니 요약
    ├── CheckoutForm.tsx   # 체크아웃 폼
    └── OrderSummary.tsx   # 주문 요약
```

---

## 🔧 필요한 유틸리티 함수

### 1. 주문 번호 생성

```typescript
// lib/utils/order.ts
export function generateOrderNumber(): string {
  const date = new Date().toISOString().slice(0, 10).replace(/-/g, '')
  const random = Math.floor(Math.random() * 10000).toString().padStart(4, '0')
  return `ORD-${date}-${random}`
}
```

### 2. 가격 포맷

```typescript
// lib/utils/currency.ts
export function formatPrice(price: number): string {
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: 'USD',
  }).format(price)
}
```

### 3. 날짜 포맷

```typescript
// lib/utils/date.ts
import { format } from 'date-fns'

export function formatDate(date: string): string {
  return format(new Date(date), 'PPP')
}
```

---

## 🧪 테스트 시나리오

### 1. 정상 주문 플로우
```
1. 홈에서 상품 클릭 → 상세 페이지
2. "Add to Cart" → 장바구니에 추가
3. 헤더의 Cart 클릭 → 장바구니 페이지
4. 수량 조절, 삭제 테스트
5. "Checkout" → 체크아웃 페이지
6. 모든 정보 입력
7. "Proceed to Payment" → Stripe Checkout
8. 테스트 카드로 결제 (4242 4242 4242 4242)
9. 주문 완료 페이지 확인
10. 주문 번호로 주문 조회
```

### 2. 에러 케이스 테스트
```
1. 빈 장바구니로 체크아웃 시도
2. 잘못된 주문 번호로 조회
3. 결제 실패 카드 사용
4. 폼 검증 실패 (빈 필드, 잘못된 형식)
```

---

## 📊 예상 작업 시간

| 단계 | 작업 | 예상 시간 |
|-----|------|----------|
| 1 | 장바구니 페이지 | 30-40분 |
| 2 | 상품 상세 페이지 | 30-40분 |
| 3 | 체크아웃 페이지 | 60-90분 |
| 4 | Stripe 결제 API | 60-90분 |
| 5 | Stripe 웹훅 API | 30-45분 |
| 6 | 주문 완료 페이지 | 20-30분 |
| 7 | 주문 조회 페이지 | 40-50분 |
| | **총 예상 시간** | **4-6시간** |

---

## ✅ 완료 체크리스트

### 장바구니 페이지
- [ ] 상품 목록 표시
- [ ] 수량 조절 기능
- [ ] 상품 삭제 기능
- [ ] 총 금액 계산
- [ ] 체크아웃 버튼

### 상품 상세 페이지
- [ ] 상품 정보 조회
- [ ] 상품 정보 표시
- [ ] 장바구니 추가 기능
- [ ] 404 처리

### 체크아웃 페이지
- [ ] 주문 정보 폼
- [ ] 폼 검증 (Zod)
- [ ] 주문 요약 표시
- [ ] 빈 장바구니 처리

### Stripe 결제 API
- [ ] 주문 생성 (Supabase)
- [ ] Checkout Session 생성
- [ ] 에러 처리
- [ ] 재고 확인

### Stripe 웹훅 API
- [ ] 서명 검증
- [ ] 주문 상태 업데이트
- [ ] 결제 정보 저장
- [ ] 에러 로깅

### 주문 완료 페이지
- [ ] Stripe 세션 조회
- [ ] 주문 번호 표시
- [ ] 성공 메시지

### 주문 조회 페이지
- [ ] 주문 정보 조회
- [ ] 주문 상세 표시
- [ ] 404 처리
- [ ] 주문 상태 배지

---

## 🚀 다음 단계

이 계획을 검토하신 후:

1. **승인** → 단계별로 구현 시작
2. **수정 필요** → 어떤 부분을 수정할지 알려주세요
3. **추가 기능** → 포함하고 싶은 기능이 있나요?

---

**작성일**: 2026-02-14
**프로젝트**: Joy-Flower
**Phase**: 2 - 고객 서비스 개발
